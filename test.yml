execution:
  scenario:
    script: test.jmx  # Path to your JMeter .jmx file
  concurrency: 10  # Number of concurrent users
  ramp-up: 1m  # Ramp-up time

modules:
  jmeter:
    path: D:\\apache-jmeter-5.6.3(1)\\apache-jmeter-5.6.3\\bin\\jmeter.bat  # Path to JMeter executable on Windows
